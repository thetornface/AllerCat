---
const isHome = Astro.url.pathname === '/';
const link = (anchor: string) => (isHome ? anchor : `/${anchor}`);
const cartHref = isHome ? '#buy' : '/#buy';
---

<nav class="nav">
  <div class="nav-inner">
    <a href="/" class="nav-logo" aria-label="AllerCat — на главную">
      <img class="nav-logo-mark" src="/assets/images/logo.svg" alt="AllerCat" width="150" height="158" />
    </a>

    <div class="nav-links" id="navLinks">
      <a href={link('#facts')}>Факты</a>
      <a href={link('#how')}>Как работает</a>
      <a href={link('#faq')}>FAQ</a>
      <a href={link('#buy')}>Предзаказ</a>
    </div>

    <div class="nav-right">
      {isHome ? (
        <button class="nav-cta" id="cartBtn" type="button">
          <span>Предзаказ</span>
          <svg class="nav-cta-arrow" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M5 12h14"/>
            <path d="M13 6l6 6-6 6"/>
          </svg>
        </button>
      ) : (
        <a class="nav-cta" href={cartHref}>
          <span>Предзаказ</span>
          <svg class="nav-cta-arrow" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M5 12h14"/>
            <path d="M13 6l6 6-6 6"/>
          </svg>
        </a>
      )}
      <button class="mob-menu" id="mobMenu" type="button" aria-label="Меню">
        <svg width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round">
          <path d="M3 6h18M3 12h18M3 18h18"/>
        </svg>
      </button>
    </div>
  </div>
</nav>

<script>
  // Mobile menu toggle
  const mobBtn = document.getElementById('mobMenu');
  const navLinks = document.getElementById('navLinks');
  if (mobBtn && navLinks) {
    mobBtn.addEventListener('click', () => {
      navLinks.classList.toggle('open');
    });
    // Close menu on link click
    navLinks.querySelectorAll('a').forEach((a) => {
      a.addEventListener('click', () => navLinks.classList.remove('open'));
    });
  }
</script>
